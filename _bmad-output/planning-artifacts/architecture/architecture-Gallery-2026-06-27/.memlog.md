---
scope: Gallery reliability/testing/modernization cycle (FR-1..FR-12)
purpose: build-substrate
altitude: feature
updated: 2026-06-27T15:11
---

- (decision) Create intent confirmed via deprecated shim forwarding; user selected coaching path
- (assumption) Assume implementation-only build substrate as primary artifact unless user asks for additional audience-facing output
- (decision) AD-1 adopted: Layered React app paradigm (UI components -> hooks/domain logic -> data/source adapters) as governing model for FR-1..FR-12
- (decision) AD-2 adopted: Context-owned cross-component UI state; usePhotos remains read-only derivation; mutations occur only through explicit context setters/actions
- (decision) AD-3 adopted: mandatory verification gate order lint->build->unit->e2e for each change batch; upgrade batches require all-pass before merge; lightbox/filter reliability changes must include assertion updates
- (decision) AD-4 adopted: Photo Record schema tolerance boundary with optional metadata handling in UI/hooks and normalization responsibility in data/script adapter layer
- (decision) AD-5 adopted: each Upgrade Batch has single owner and verification artifact; failing any gate triggers whole-batch rollback before next batch; no overlapping upgrade batches
- (change) Reconciled spine against PRD and project-context inputs; no blocking gaps, preference defer for checklist location retained
- (decision) AD-6 added from adversarial review: filter/lightbox synchronization invariant to prevent stale index/state mismatches
- (change) Reviewer gate completed: lint_spine passed with zero findings; adversarial and version checks documented in review files
- (event) spine finalized
